import { type DetectedBarcode } from '@sec-ant/barcode-detector';
/**
 * Continuously extracts frames from camera stream and tries to read
 * potentially pictured QR codes.
 */
export declare const keepScanning: (videoElement: HTMLVideoElement, { detectHandler, locateHandler, minDelay }: {
    detectHandler: any;
    locateHandler: any;
    minDelay: any;
}) => Promise<void>;
export declare const processFile: (file: File) => Promise<DetectedBarcode[]>;
export declare const processUrl: (url: string) => Promise<DetectedBarcode[]>;
